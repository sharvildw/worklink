# Backend Setup

1. `cd backend`
2. `npm install`
3. Copy `.env.example` to `.env`
4. `npm run seed`
5. `npm run dev`

## Demo Accounts

- Admin: `admin@worklink.in` / `password123`
- User: `rohan.patil@example.com` / `password123`
- Worker: `amit.shinde@example.com` / `password123`

## Core API Groups

- `/api/auth`
- `/api/users`
- `/api/workers`
- `/api/jobs`
- `/api/admin`
